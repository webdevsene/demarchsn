[build]
  publish = "exampleSite/public"
  # command = "cd exampleSite && hugo --gc --themesDir ../.."
  # command = "hugo --minify --themesDir ../.."
  command = "hugo --minify --themesDir themes"
  
[build.environment]
  HUGO_VERSION = "0.87.0"
  # HUGO_THEME = "repo"
  HUGO_THEME = "meghna-hugo"
  # HUGO_BASEURL = "/"
  HUGO_BASEURL = "https://"